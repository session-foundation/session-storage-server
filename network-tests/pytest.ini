[pytest]
norecursedirs=*
# The storage servers' certificates are self-signed and deliberately not verified; pytest would
# otherwise re-enable this warning for every HTTPS request.
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
